<?xml version="1.0" encoding="utf-8"?>
<!-- 1. MaterialCardView als Hauptcontainer für runde Ecken und Schatten -->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp"
    app:strokeWidth="0dp"
    android:backgroundTint="@color/card_bg"
    android:clickable="true"
    android:focusable="true">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- 2. Bild angepasst: Volle Höhe, linker Rand, keine runde Form mehr (übernimmt die der Karte) -->
        <ImageView
            android:id="@+id/image_news"
            android:layout_width="90dp"
            android:layout_height="0dp"
            android:scaleType="centerCrop"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@color/button_bg" />

        <!-- 3. Titel angepasst: Startet jetzt rechts vom Bild -->
        <TextView
            android:id="@+id/text_news_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:layout_marginTop="16dp"
            android:textAppearance="@style/TextAppearance.Material3.TitleMedium"
            app:layout_constraintEnd_toStartOf="@+id/icon_arrow_right"
            app:layout_constraintStart_toEndOf="@+id/image_news"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Tag der Niedersachsen" />

        <!-- 4. Distanz angepasst: Startet auch rechts vom Bild -->
        <TextView
            android:id="@+id/text_news_distance"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:layout_marginBottom="16dp"
            android:textAppearance="@style/TextAppearance.Material3.BodyMedium"
            android:textColor="?android:attr/textColorSecondary"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="@+id/text_news_title"
            app:layout_constraintStart_toStartOf="@+id/text_news_title"
            app:layout_constraintTop_toBottomOf="@+id/text_news_title"
            tools:text="4 km" />

        <!-- Pfeil bleibt unverändert -->
        <ImageView
            android:id="@+id/icon_arrow_right"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="16dp"
            android:src="@drawable/ic_arrow_right"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="?attr/colorOnSurface" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
